
# Environment variables require
`OUTPUT_DIR` : for data directory

# How to Run app 
1. *CLONE* repository to your machine
2. *MOVE* in terminal go to local version repo
3. *Virtual Environment* `pipenv install` + `pipenv shell`









