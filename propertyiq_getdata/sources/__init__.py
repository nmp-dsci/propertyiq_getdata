"""One module per collected source.

Each source module exposes an ``update_*`` entrypoint plus manifest and
legacy-compatibility helpers, and depends only on
:mod:`propertyiq_getdata.core` for shared mechanics.
"""
